const styles = (theme) => ({
	content: {
		padding: theme.spacing.unit * 2,
		width: "100%"
	},
	qrCodeContainer: {
		width: "50%",
		margin: "auto",
		padding: theme.spacing.unit * 2
	}
});

export default styles;

const styles = (theme) => ({
	root: {
		width: "100%",
		minWidth: "35vw",
		padding: theme.spacing.unit
	},
	form: {

	},
	recentAddresses: {
		backgroundColor: theme.palette.background.paper,
	},
	recentAddressesList: {
		backgroundColor: theme.palette.background.paper
	}
});

export default styles;

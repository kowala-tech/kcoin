const styles = (theme) => ({
	root: {
		width: "100vw"
	},
	recentAddresses: {
		backgroundColor: theme.palette.background.paper,
	},
	recentAddressesList: {
		backgroundColor: theme.palette.background.paper
	}
});

export default styles;

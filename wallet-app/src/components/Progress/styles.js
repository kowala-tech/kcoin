const styles = (theme) => ({
	root: {
		padding: theme.spacing.unit * 2,
		backgroundColor: "inherit",
		display: "flex",
		alignItems: "center",
		justifyContent: "center"
	}
});

export default styles;

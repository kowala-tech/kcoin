const styles = (theme) => ({
	root: {
	},
	input: {
		position: "absolute",
		top: -1000
	},
	pinDigit: {
		marginRight: theme.spacing.unit * 2,
		marginLeft: theme.spacing.unit * 2
	}
});

export default styles;

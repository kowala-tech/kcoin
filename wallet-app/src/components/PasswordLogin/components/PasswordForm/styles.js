const styles = (theme) => ({
	buttonWrapper: {
		position: "relative",
		marginTop: theme.spacing.unit * 2
	},
	buttonProgress: {
		position: "absolute",
		top: "50%",
		left: "50%",
		marginTop: -12,
		marginLeft: -12,
	}
});

export default styles;
